const Placeholder = ({ title }: { title: string }) => (
  <div className="p-8">
    <h1 className="text-2xl font-bold">Page: {title}</h1>
    <p className="text-gray-500">This route is currently under construction.</p>
  </div>
);

export default Placeholder;
